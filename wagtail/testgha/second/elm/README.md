# Elm car search and filter app

## Compile new code

These commands should be run from the same directory as this README file.

### Dev
```bash
make develop
```

### Optimising ###
Adds the `--optimize` compiler flag to elm-make and then feed through `uglifyjs`

```bash
make release
```
